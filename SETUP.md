# 📖 คู่มือติดตั้งทีละขั้น — ระบบสต็อกครัวกลางหม่าล่า

ระบบนี้ใช้ของฟรีทั้งหมด 3 อย่าง:
1. **Google Sheet** — เก็บข้อมูล
2. **Google Apps Script** — เว็บแอป + ตัวส่ง LINE (ฝังอยู่ในชีต)
3. **LINE Official Account (Messaging API)** — ส่งแจ้งเตือนเข้ากลุ่มสาขา

ใช้เวลาตั้งค่าประมาณ 20–30 นาที ทำครั้งเดียว จากนั้นใช้งานได้เลย

---

## ส่วนที่ 1 — สร้าง Google Sheet + วางโค้ด

1. เปิด https://sheets.google.com สร้างสเปรดชีตใหม่ 1 ไฟล์ ตั้งชื่อเช่น **"สต็อกหม่าล่า"**
2. เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. จะเปิดหน้าเขียนโค้ดขึ้นมา ให้ทำดังนี้:
   - ลบโค้ดเดิมใน `Code.gs` ทิ้ง แล้ววางเนื้อหาจากไฟล์ **`apps-script/Code.gs`** ลงไป
   - กดปุ่ม **+** ข้างคำว่า "Files" → **HTML** สร้างไฟล์ทีละอัน (ชื่อต้องตรงเป๊ะ ไม่ต้องใส่ .html):
     - `Style`    → วางเนื้อหาจาก `apps-script/Style.html`
     - `Index`    → วางเนื้อหาจาก `apps-script/Index.html`
     - `Entry`    → วางเนื้อหาจาก `apps-script/Entry.html`
     - `Pack`     → วางเนื้อหาจาก `apps-script/Pack.html`
     - `StockIn`  → วางเนื้อหาจาก `apps-script/StockIn.html`
     - `Auth`     → วางเนื้อหาจาก `apps-script/Auth.html`
   - (ถ้าเห็นเมนู Project Settings → เปิด "Show appsscript.json") สามารถวางค่าจาก `apps-script/appsscript.json` ทับได้ เพื่อให้ timezone/scopes ตรง — ไม่บังคับ
4. กด **บันทึก** (ไอคอนแผ่นดิสก์) ทั้งหมด

---

## ส่วนที่ 2 — ติดตั้งครั้งแรก (สร้างชีตอัตโนมัติ)

1. กลับไปที่หน้า Google Sheet **ปิดแล้วเปิดใหม่** 1 รอบ (เพื่อให้เมนูโผล่)
2. จะเห็นเมนูใหม่ชื่อ **"🌶️ ระบบสต็อก"** ด้านบน
3. กด **🌶️ ระบบสต็อก → 1) ติดตั้งครั้งแรก**
   - ครั้งแรกจะมีหน้าต่างขอสิทธิ์ (Authorization) → กด **Review permissions → เลือกบัญชี → Advanced → Go to ... (unsafe) → Allow**
     (ปลอดภัย เพราะเป็นสคริปต์ของคุณเอง)
4. ระบบจะสร้างชีตให้อัตโนมัติ: `รายการสินค้า`, `สาขา`, `ผู้ใช้งาน`, `บันทึกโอน`, `รับเข้าครัวกลาง`, `สรุป`, `สรุปสาขา`, `LINE_IDs`

### 🔐 ผู้ใช้งานและรหัสผ่าน (สำคัญ)
ระบบสร้างบัญชีตัวอย่างให้ 3 บัญชี **รหัสผ่าน `1234` ทุกบัญชี — ต้องเปลี่ยนก่อนใช้จริง**

| ชื่อผู้ใช้ | บทบาท | ทำอะไรได้ |
|---|---|---|
| `admin` | แอดมิน | ทุกอย่าง + ดูสรุปทุกสาขา |
| `kitchen` | ครัวกลาง | รับเข้า + ส่งออกไปสาขา + ดูสรุป |
| `branch1` | สาขา | กรอกจำนวนที่แพ็คได้ **เฉพาะสาขาตัวเอง** |

**วิธีเพิ่ม/แก้ผู้ใช้** — แก้ที่ชีต **`ผู้ใช้งาน`** โดยตรง:

| ชื่อผู้ใช้ | รหัสผ่าน | ชื่อ-สกุล | บทบาท | สาขา | เปิดใช้งาน |
|---|---|---|---|---|---|
| branch2 | รหัสใหม่ | พนง.สาขา 2 | สาขา | สาขา 2 | ✅ |

- **รหัสผ่าน**: พิมพ์เป็นตัวอักษรธรรมดาลงไปได้เลย ระบบจะ**แปลงเป็นรหัสเข้ารหัส (hash) อัตโนมัติ**ตอนล็อกอินครั้งแรก — หลังจากนั้นจะอ่านรหัสจริงจากชีตไม่ได้อีก
- **เปลี่ยนรหัส**: พิมพ์รหัสใหม่ทับช่องเดิม (ทับ hash ได้เลย)
- **ปิดบัญชีชั่วคราว**: เอาเครื่องหมายถูกในช่อง "เปิดใช้งาน" ออก
- **ช่อง "สาขา"**: ใส่เฉพาะบทบาท `สาขา` เท่านั้น และต้องสะกดตรงกับชีต `สาขา` เป๊ะ ๆ
- **บังคับทุกคนล็อกอินใหม่**: เมนู **🔒 บังคับล็อกอินใหม่ทุกคน**

> พนักงานล็อกอินครั้งเดียวแล้วเครื่องจะจำไว้ **30 วัน** ไม่ต้องล็อกอินทุกครั้งที่กดลิงก์จาก LINE

### กรอกข้อมูลตั้งต้น
- ชีต **`รายการสินค้า`** — **ใส่รายการจริงให้แล้ว 36 รายการ** ตรวจทาน/แก้ไข/เพิ่มได้เลย
- ชีต **`สาขา`** — ใส่ชื่อสาขาทั้งหมด (คอลัมน์ LINE Group ID เว้นไว้ก่อน จะเติมในส่วนที่ 4)

### 📏 สินค้ารองรับ 2 แบบ (สำคัญ)
ชีต `รายการสินค้า` มีคอลัมน์ **หน่วยวัด** ให้เลือก 2 แบบ:

| หน่วยวัด | ใช้กับ | ปริมาณต่อหน่วย | ครัวกลางกรอกเป็น | ตัวอย่าง |
|---|---|---|---|---|
| **กรัม** | ของชั่งน้ำหนัก | กรัมต่อ 1 ถุง | กิโล / กรัม | สันคอ `30` → 1 กิโล = 33 ถุง |
| **ชิ้น** | ของนับชิ้น | ชิ้นต่อ 1 ไม้/ถุง | จำนวนชิ้น | ปูอัด `2` → 100 ชิ้น = 50 ไม้ |

> ⚠️ **ต้องเลือกหน่วยวัดให้ถูก** — ถ้าสินค้านับชิ้นแต่ไปตั้งเป็น "กรัม" ระบบจะคำนวณผิดมหาศาล
> (เช่น ปูอัด ตั้งเป็นกรัม → ส่ง 1 โล จะได้ 500 ไม้ แทนที่จะเป็น 50)

คอลัมน์ **หน่วยขาย** คือคำที่จะแสดงในแจ้งเตือน — ใส่ `ถุง` หรือ `ไม้` ตามจริง

---

## ส่วนที่ 3 — เปิดเป็นเว็บแอป (Deploy)

1. ที่หน้า Apps Script กดปุ่ม **Deploy → New deployment**
2. ไอคอนเฟือง ⚙️ ข้าง "Select type" → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** `Me` (ตัวคุณเอง)
   - **Who has access:** `Anyone` ← สำคัญ เพื่อให้พนักงานเปิดลิงก์จาก LINE ได้โดยไม่ต้องมีบัญชี Google
     (ยังต้องล็อกอินด้วย ชื่อผู้ใช้/รหัสผ่าน ของระบบอยู่ — ตั้งที่ชีต `ผู้ใช้งาน`)
4. กด **Deploy** → คัดลอก **Web app URL** (ลงท้ายด้วย `/exec`) เก็บไว้
   - ดูลิงก์นี้ได้ทีหลังที่เมนู **🌶️ ระบบสต็อก → แสดงลิงก์เว็บแอป**
5. เปิดลิงก์นี้ = หน้าแรกของระบบ (บุ๊กมาร์ก/ปักหมุดไว้ที่ครัวกลางได้เลย)

> ⚠️ ทุกครั้งที่แก้โค้ด ต้องกด **Deploy → Manage deployments → (ดินสอ) → Version: New version → Deploy** เพื่อให้เว็บอัปเดต

---

## ส่วนที่ 4 — ตั้งค่า LINE Official Account (Messaging API)

> ### ♻️ มีระบบแจ้ง "วันหมดอายุ" บน LINE อยู่แล้ว? ใช้ LINE เดิมร่วมกันได้เลย
> ระบบนี้แค่ **เพิ่มการแจ้ง "ของเข้า"** เข้าไปในกลุ่มเดิม โดย **ไม่ต้องแตะระบบแจ้งวันหมดอายุเดิม**
> - **ใช้กลุ่ม LINE เดิมของสาขา** ได้เลย (กลุ่มเดียวรับได้ทั้งของเข้าและวันหมดอายุ)
> - **ถ้าระบบเดิมเป็น Messaging API (OA):** ใช้ **Channel access token เดิม** ได้เลย → ข้ามข้อ 4.1 ไปใส่ token เดิมที่ข้อ 4.2
> - ⚠️ **ข้ามข้อ 4.3 (Webhook) ไปเลย** เพื่อไม่ให้ไปทับ webhook ของระบบเดิม (1 channel ตั้ง webhook ได้อันเดียว)
> - **Group ID:** คุณมีอยู่แล้วจากระบบเดิม → เอาไปวางในชีต **"สาขา"** ตรงๆ (ข้อ 4.4 แบบ "กรอกเอง")
>   - ถ้าไม่รู้ Group ID: ดูจากที่ระบบเดิมตั้งไว้ หรือทำตามข้อ 4.3–4.4 ชั่วคราวเพื่อดึง ID มา แล้วค่อยปิด webhook คืนให้ระบบเดิม
> - **ถ้าระบบเดิมเป็นบริการอื่น** (Make/Zapier/add-on/บอทเจ้าอื่น): สร้าง OA ใหม่ตามข้อ 4.1 แล้วเชิญบอทใหม่เข้ากลุ่มเดิม — จะมี 2 บอทในกลุ่ม ต่างคนต่างแจ้ง ใช้ได้ปกติ
>
> **สรุปทางลัด (แนะนำ):** ใช้ token เดิม + ข้าม 4.3 + วาง Group ID เดิมลงชีต "สาขา" เอง = เสร็จ

### 4.1 สร้าง OA และเปิด Messaging API  *(ข้ามได้ถ้าจะใช้ OA เดิม)*
1. ไป https://developers.line.biz/console/ ล็อกอินด้วยบัญชี LINE
2. สร้าง **Provider** (ชื่ออะไรก็ได้ เช่น "หม่าล่า")
3. สร้าง **Channel → Messaging API** (จะได้ Official Account มาด้วย)
4. เข้าแท็บ **Messaging API**:
   - เลื่อนหา **Channel access token (long-lived)** → กด **Issue** → คัดลอก token เก็บไว้
5. แท็บ **Messaging API** ปิดสองอย่างนี้เพื่อกันบอทตอบมั่ว: **Auto-reply messages = Disabled**, **Greeting messages** ตามใจ
   (ตั้งได้ที่ปุ่มลิงก์ "LINE Official Account Manager")

### 4.2 ใส่ token เข้าสคริปต์
1. ที่หน้า Apps Script → เมนูซ้าย **Project Settings (เฟือง) → Script Properties → Add script property**
2. ใส่:
   - **Property:** `LINE_TOKEN`
   - **Value:** วาง Channel access token (จะเป็น token ของ OA เดิมหรือ OA ใหม่ก็ได้)
3. กด **Save**

### 4.3 ตั้ง Webhook — *ข้ามได้ / ไม่จำเป็น*
> ⚠️ **ถ้าใช้ LINE เดิมที่มีระบบแจ้งวันหมดอายุอยู่ ให้ข้ามข้อนี้** (การตั้ง webhook ใหม่จะไปทับของเดิม)
> webhook ใช้แค่เพื่อ "จับ Group ID อัตโนมัติ" เท่านั้น ถ้ามี Group ID อยู่แล้วก็ไม่ต้องตั้ง

*(เฉพาะกรณีสร้าง OA ใหม่ และอยากให้ระบบจับ Group ID ให้)*
1. แท็บ **Messaging API** → ช่อง **Webhook URL** → วาง **Web app URL** (`/exec`) → กด **Update**
2. เปิดสวิตช์ **Use webhook = เปิด**

### 4.4 ใส่ Group ID ของแต่ละสาขาลงชีต "สาขา"

**วิธี A — กรอกเอง (แนะนำ ถ้าใช้ LINE เดิม):**
- เอา Group ID ที่คุณมีอยู่แล้ว (จากระบบแจ้งวันหมดอายุเดิม) ไปวางในชีต **`สาขา`** คอลัมน์ **LINE Group ID** ให้ตรงกับชื่อสาขา — เสร็จ

**วิธี B — ให้ระบบจับให้ (เฉพาะกรณีตั้ง webhook ตามข้อ 4.3):**
1. เชิญ **Official Account** เข้ากลุ่มสาขา
2. พิมพ์คำว่า **`id`** ในกลุ่ม → บอทจะตอบ **Group ID** (ขึ้นต้นด้วย `C...`) กลับมา
3. คัดลอกไปวางในชีต **`สาขา`** คอลัมน์ **LINE Group ID**
4. ทำซ้ำทุกสาขา (Group ID ที่จับได้จะถูกเก็บในชีต `LINE_IDs` ด้วย)

### 4.5 ทดสอบการเชื่อมต่อ
กด **🌶️ ระบบสต็อก → 🧪 ทดสอบส่ง LINE ทุกสาขา**
ระบบจะส่งข้อความทดสอบเข้าทุกกลุ่ม แล้วสรุปผลว่าสาขาไหนสำเร็จ/ไม่สำเร็จ
(เป็นการ push อย่างเดียว **ไม่กระทบ webhook หรือระบบแจ้งวันหมดอายุเดิม**)

---

## 🔁 กรณีเฉพาะ: มีระบบแจ้ง "วันหมดอายุ" บน Messaging API OA อยู่แล้ว

ทำตาม 3 ขั้นนี้พอ — **ระบบเดิมทำงานต่อได้เหมือนเดิมทุกอย่าง**

**ขั้นที่ 1 — เอา token เดิมมาใช้**
1. เข้า https://developers.line.biz/console/ → เลือก Provider → เลือก **Channel เดิม** ที่ใช้แจ้งวันหมดอายุ
2. แท็บ **Messaging API** → เลื่อนลงล่างสุด **Channel access token (long-lived)** → กด **Copy**
   - ⚠️ **อย่ากด Issue ใหม่** ถ้ามี token อยู่แล้ว เพราะ token เดิมจะใช้ไม่ได้ → ระบบแจ้งวันหมดอายุจะพัง
   - ถ้าปุ่มขึ้นว่า Issue (ยังไม่เคยออก token) แปลว่าระบบเดิมอาจใช้วิธีอื่น ให้ตรวจสอบก่อน
3. เอา token ไปใส่ใน Script Properties คีย์ `LINE_TOKEN` (ตามข้อ 4.2)

**ขั้นที่ 2 — อย่าแตะ Webhook** ⚠️
- ปล่อยช่อง Webhook URL ไว้เหมือนเดิม **ห้ามเปลี่ยน** (1 channel ตั้งได้อันเดียว ถ้าเปลี่ยนระบบเดิมจะพัง)
- ระบบนี้ไม่ต้องใช้ webhook ก็ทำงานได้ครบ (webhook มีไว้จับ Group ID อัตโนมัติเท่านั้น)

**ขั้นที่ 3 — ใส่ Group ID ของแต่ละสาขา**
- ไปดูว่าระบบแจ้งวันหมดอายุเดิมส่งเข้ากลุ่มไหน → คัดลอก **Group ID** (ขึ้นต้นด้วย `C`) จากการตั้งค่าของระบบเดิม
- วางลงชีต **`สาขา`** คอลัมน์ **LINE Group ID** ให้ตรงกับชื่อสาขา
- กด **🧪 ทดสอบส่ง LINE ทุกสาขา** เพื่อยืนยันว่าถูกต้อง

> **หา Group ID เดิมไม่เจอ?** ตั้ง webhook ชั่วคราวได้ แต่ต้อง **คัดลอก Webhook URL เดิมเก็บไว้ก่อน**
> → เปลี่ยนเป็น URL ของระบบนี้ → พิมพ์ `id` ในแต่ละกลุ่มเพื่อเก็บ ID → **แล้วเปลี่ยน Webhook URL กลับเป็นของเดิมทันที**

**ผลลัพธ์:** บอทตัวเดิม กลุ่มเดิม แจ้งทั้ง "วันหมดอายุ" (ระบบเดิม) และ "ของเข้า/แพ็ค" (ระบบนี้)

---

## ✅ เสร็จแล้ว! ทดสอบการใช้งาน (ครบวงจร)

1. เปิด **Web app URL** → กด **"📥 รับเข้าครัวกลาง"** → บันทึกของที่ซัพพลายเออร์ส่งเข้ามา (เช่น สันคอ 10 กิโล)
   → เพิ่มยอด **คงเหลือครัวกลาง**
2. กด **"📤 ส่งออกไปสาขา"** → เลือกสาขา + สินค้า (สันคอ) + น้ำหนัก (1 กิโล)
   → ระบบโชว์ "ควรแพ็คได้ ~33 ถุง" และคงเหลือครัวกลางที่จะลดลง
3. กด **บันทึก + แจ้ง LINE สาขา**
4. กลุ่ม LINE ของสาขาจะได้ข้อความพร้อม **ลิงก์** → พนักงานกดลิงก์ → กรอกจำนวนที่แพ็คได้จริง
5. ระบบคำนวณ **ของหาย** (ควรได้ − แพ็คจริง) แล้วบันทึกลงชีต `บันทึกโอน`
   + อัปเดต `สรุป` (รวมคงเหลือครัวกลาง) และ `สรุปสาขา`

### วงจรข้อมูลทั้งหมด
```
รับเข้าครัวกลาง ──▶ คงเหลือครัวกลาง ──▶ ส่งออกไปสาขา ──▶ แจ้ง LINE ──▶ สาขาแพ็ค ──▶ ของหาย
   (StockIn)          (Balance)          (Transfer)                   (Pack)      (Loss)
```
ดูสรุปทั้งหมดได้ที่หน้าแรกของเว็บ (Index) และในชีต `สรุป` / `สรุปสาขา`

---

## ❓ ปัญหาที่พบบ่อย

| อาการ | วิธีแก้ |
|---|---|
| บอทไม่ตอบ "id" ในกลุ่ม | ปิด Auto-reply ใน OA Manager, เปิด Use webhook, เช็ค Webhook URL ลงท้าย `/exec` |
| เว็บขึ้น "ยังไม่ได้ส่ง LINE" | ยังไม่ได้ใส่ `LINE_TOKEN` หรือช่อง LINE Group ID ในชีตสาขายังว่าง |
| แก้โค้ดแล้วเว็บไม่เปลี่ยน | ต้อง Deploy เวอร์ชันใหม่ (Manage deployments → New version) |
| ไม่โชว์ "ควรแพ็คได้กี่ถุง" | สินค้านั้นยังไม่ได้ใส่ค่า "กรัมต่อถุง" ในชีต `รายการสินค้า` |
| เปิดลิงก์แล้วให้ล็อกอินด้วย Google | ตอน Deploy ต้องตั้ง Who has access = **Anyone** |
| ลืมรหัสผ่านพนักงาน | พิมพ์รหัสใหม่ทับในชีต `ผู้ใช้งาน` ช่องรหัสผ่าน (ทับ hash ได้เลย) |
| สาขาเปิดลิงก์แล้วขึ้น "ไม่มีสิทธิ์" | ช่อง "สาขา" ในชีต `ผู้ใช้งาน` สะกดไม่ตรงกับชีต `สาขา` |

## 🔒 ความปลอดภัย — อ่านก่อนใช้จริง
- **เปลี่ยนรหัสผ่านเริ่มต้น (`1234`) ทุกบัญชีก่อนเปิดใช้งานจริง** ไม่งั้นใครเดาได้ก็เข้าระบบได้
- ลิงก์เว็บต้องตั้งเป็น "Anyone" เพื่อให้เปิดจาก LINE ได้ → **ความปลอดภัยจึงอยู่ที่รหัสผ่าน** ตั้งให้เดายาก และอย่าส่งลิงก์+รหัสไว้ในกลุ่มเดียวกัน
- รหัสผ่านถูกเก็บเป็น hash (SHA-256 + salt) ไม่ได้เก็บรหัสจริง
- **ใครเข้าถึง Google Sheet ได้ = เห็นและแก้ข้อมูลทั้งหมดได้** โดยไม่ต้องผ่านหน้าล็อกอิน → แชร์ชีตเฉพาะแอดมินเท่านั้น
- Channel access token = รหัสลับ อย่าเผยแพร่ อย่า commit ลง GitHub (เก็บใน Script Properties เท่านั้น)
- พนักงานที่ลาออก: เอาเครื่องหมายถูก "เปิดใช้งาน" ออก แล้วกดเมนู **🔒 บังคับล็อกอินใหม่ทุกคน**
