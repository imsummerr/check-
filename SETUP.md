# 📖 คู่มือติดตั้งทีละขั้น — ระบบสต็อกครัวกลางหม่าล่า

ระบบนี้ใช้ของฟรีทั้งหมด 3 อย่าง:
1. **Google Sheet** — เก็บข้อมูล
2. **Google Apps Script** — เว็บแอป + ตัวส่ง LINE (ฝังอยู่ในชีต)
3. **LINE Official Account (Messaging API)** — ส่งแจ้งเตือนเข้ากลุ่มครัวกลางและกลุ่มสาขา

ใช้เวลาตั้งค่าประมาณ 20–30 นาที ทำครั้งเดียว จากนั้นใช้งานได้เลย

---

## ส่วนที่ 1 — สร้าง Google Sheet + วางโค้ด

1. เปิด https://sheets.google.com สร้างสเปรดชีตใหม่ 1 ไฟล์ ตั้งชื่อเช่น **"สต็อกหม่าล่า"**
2. เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. จะเปิดหน้าเขียนโค้ดขึ้นมา ให้ทำดังนี้:
   - ลบโค้ดเดิมใน `Code.gs` ทิ้ง แล้ววางเนื้อหาจากไฟล์ **`apps-script/Code.gs`** ลงไป
   - กดปุ่ม **+** ข้างคำว่า "Files" → **HTML** สร้างไฟล์ทีละอัน (ชื่อต้องตรงเป๊ะ ไม่ต้องใส่ .html):
     - `Style`    → `apps-script/Style.html`
     - `Auth`     → `apps-script/Auth.html`
     - `Common`   → `apps-script/Common.html`
     - `Index`    → `apps-script/Index.html`
     - `StockIn`  → `apps-script/StockIn.html`
     - `ToShop`   → `apps-script/ToShop.html`
     - `Count`    → `apps-script/Count.html`
     - `Waste`    → `apps-script/Waste.html`
     - `Pos`      → `apps-script/Pos.html`
     - `Report`   → `apps-script/Report.html`
   - (ถ้าเห็นเมนู Project Settings → เปิด "Show appsscript.json") สามารถวางค่าจาก `apps-script/appsscript.json` ทับได้ เพื่อให้ timezone/scopes ตรง — ไม่บังคับ
4. กด **บันทึก** (ไอคอนแผ่นดิสก์) ทั้งหมด

---

## ส่วนที่ 2 — ติดตั้งครั้งแรก (สร้างชีตอัตโนมัติ)

1. กลับไปที่หน้า Google Sheet **ปิดแล้วเปิดใหม่** 1 รอบ (เพื่อให้เมนูโผล่)
2. จะเห็นเมนูใหม่ชื่อ **"🌶️ ระบบสต็อก"** ด้านบน
3. กด **🌶️ ระบบสต็อก → 1) ติดตั้งครั้งแรก**
   - ครั้งแรกจะมีหน้าต่างขอสิทธิ์ (Authorization) → กด **Review permissions → เลือกบัญชี → Advanced → Go to ... (unsafe) → Allow**
     (ปลอดภัย เพราะเป็นสคริปต์ของคุณเอง)
4. ระบบจะสร้างชีตให้อัตโนมัติ: `รายการสินค้า`, `สถานที่`, `ผู้ใช้งาน`, `ของเข้าครัวกลาง`, `ของเข้าร้าน`, `ของเสีย`, `เช็คสต็อก`, `ยอด POS`, `สรุปสต็อก`, `LINE_IDs`
5. กด **🌶️ ระบบสต็อก → 2) เปิดแจ้งเตือนวันหมดอายุอัตโนมัติ** (ทำครั้งเดียว)
   → เปิดให้ทั้ง **วันหมดอายุ** และ **ของครัวกลางใกล้หมด** พร้อมกัน

### 🔐 ผู้ใช้งานและรหัสผ่าน (สำคัญ)
ระบบสร้างบัญชีตัวอย่างให้ 3 บัญชี **รหัสผ่าน `1234` ทุกบัญชี — ต้องเปลี่ยนก่อนใช้จริง**

| ชื่อผู้ใช้ | บทบาท | ทำอะไรได้ |
|---|---|---|
| `admin` | แอดมิน | ทุกอย่าง ทุกสถานที่ |
| `kitchen` | ครัวกลาง | ของเข้าครัวกลาง + ของเข้าร้าน + เช็คสต็อก + ของเสีย |
| `branch1` | สาขา | เช็คสต็อก + ของเสีย **เฉพาะสาขาตัวเอง** |

**วิธีเพิ่ม/แก้ผู้ใช้** — แก้ที่ชีต **`ผู้ใช้งาน`** โดยตรง:

| ชื่อผู้ใช้ | รหัสผ่าน | ชื่อ-สกุล | บทบาท | สาขา | เปิดใช้งาน |
|---|---|---|---|---|---|
| branch2 | รหัสใหม่ | พนง.สาขา 2 | สาขา | สาขา 2 | ✅ |

- **รหัสผ่าน**: พิมพ์เป็นตัวอักษรธรรมดาลงไปได้เลย ระบบจะ**แปลงเป็นรหัสเข้ารหัส (hash) อัตโนมัติ**ตอนล็อกอินครั้งแรก — หลังจากนั้นจะอ่านรหัสจริงจากชีตไม่ได้อีก
- **เปลี่ยนรหัส**: พิมพ์รหัสใหม่ทับช่องเดิม (ทับ hash ได้เลย)
- **ปิดบัญชีชั่วคราว**: เอาเครื่องหมายถูกในช่อง "เปิดใช้งาน" ออก
- **ช่อง "สาขา"**: ใส่เฉพาะบทบาท `สาขา` เท่านั้น และต้องสะกดตรงกับชีต `สถานที่` เป๊ะ ๆ
- **บังคับทุกคนล็อกอินใหม่**: เมนู **🔒 บังคับล็อกอินใหม่ทุกคน**

> พนักงานล็อกอินครั้งเดียวแล้วเครื่องจะจำไว้ **30 วัน** ไม่ต้องล็อกอินทุกครั้งที่กดลิงก์จาก LINE


### 📏 ตั้งค่าหน่วยนับ (สำคัญที่สุด)

แต่ละสินค้าในชีต `รายการสินค้า` ตั้ง 2 หน่วย:

| คอลัมน์ | คืออะไร | ตัวอย่าง |
|---|---|---|
| **หน่วยย่อย** | หน่วยเล็กสุดที่นับ | `ไม้` / `กรัม` / `ชิ้น` |
| **หน่วยแพ็ค** | หน่วยที่แพ็คขาย | `แพ็ค` / `ถุง` |
| **หน่วยย่อยต่อแพ็ค** | 1 แพ็คมีกี่หน่วยย่อย | ไส้กรอกหนังกรอบ = `7` (7 ไม้/แพ็ค) |
| **ราคาขาย/หน่วยย่อย** | ราคาต่อ 1 ไม้/กรัม/ชิ้น | `10` (บาท/ไม้) |
| **อายุเก็บ(วัน)** | ใช้คำนวณวันหมดอายุอัตโนมัติ | `5` |
| **เตือนเมื่อเหลือ(แพ็ค)** | ของครัวกลางเหลือถึงเท่านี้ → แจ้ง LINE | `10` (ใส่ `0` = ไม่เตือน) |

> ⚠️ **ราคาตั้งต้นเป็น 0 ทุกรายการ** — ต้องใส่ราคาจริงก่อน ไม่งั้นรายงาน "ตรวจเช็คของหาย" จะคิดเงินไม่ได้
> (รายงานจะเตือนให้เองว่ารายการไหนยังไม่มีราคา)

> ⚠️ **ค่าตั้งต้นที่ใส่มาให้เป็นค่าเดา** — ของเสียบไม้ใส่ 7 ไม้/แพ็ค และอายุเก็บ 2–90 วัน
> **ต้องแก้ให้ตรงกับที่ร้านใช้จริงทุกรายการ** (คอลัมน์หมายเหตุเขียนว่า "ตรวจสอบตัวเลข" ไว้เตือน)

**ตัวอย่างการทำงาน** — ลูกชิ้น 1 ถุง 26 ชิ้น เสียบได้ 26 ไม้ แพ็คละ 7 ไม้:
```
พนักงานเสียบ+แพ็คเสร็จ → ได้ 3 แพ็ค เหลือ 5 ไม้
    ↓ กรอกในหน้า "ของเข้าครัวกลาง":  [3] แพ็ค + [5] ไม้
ระบบเก็บเป็น 26 ไม้ แล้วแจ้งกลุ่มครัวกลางว่า "3 แพ็ค 5 ไม้"
    ↓ ส่งเข้าร้าน 1 แพ็ค
ระบบรู้ว่า = 7 ไม้ → ครัวกลางเหลือ 2 แพ็ค 5 ไม้
แจ้งกลุ่มสาขา: "1 แพ็ค (1 แพ็ค = 7 ไม้) วันที่เข้า … หมดอายุ …"
```

### 🏬 ชีต `สถานที่` — ตั้งกลุ่ม LINE

| สถานที่ | ประเภท | LINE Group ID |
|---|---|---|
| ครัวกลาง | ครัวกลาง | `C...` ของกลุ่มครัวกลาง |
| สาขา 1 | สาขา | `C...` ของกลุ่มสาขา 1 |
| สาขา 2 | สาขา | `C...` ของกลุ่มสาขา 2 |

**แต่ละแถวคือหนึ่งกลุ่ม LINE** — ของเข้าครัวกลางแจ้งเข้ากลุ่มครัวกลาง, ของเข้าร้าน/ของเสีย/วันหมดอายุของสาขาไหน แจ้งเข้ากลุ่มสาขานั้น

> ต้องมีแถวประเภท `ครัวกลาง` อย่างน้อย 1 แถว

## ส่วนที่ 3 — เปิดเป็นเว็บแอป (Deploy)

1. ที่หน้า Apps Script กดปุ่ม **Deploy → New deployment**
2. ไอคอนเฟือง ⚙️ ข้าง "Select type" → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** `Me` (ตัวคุณเอง)
   - **Who has access:** `Anyone` ← สำคัญ เพื่อให้พนักงานเปิดลิงก์จาก LINE ได้โดยไม่ต้องมีบัญชี Google
     (ยังต้องล็อกอินด้วย ชื่อผู้ใช้/รหัสผ่าน ของระบบอยู่ — ตั้งที่ชีต `ผู้ใช้งาน`)
4. กด **Deploy** → คัดลอก **Web app URL** (ลงท้ายด้วย `/exec`) เก็บไว้
   - ดูลิงก์นี้ได้ทีหลังที่เมนู **🌶️ ระบบสต็อก → แสดงลิงก์เว็บแอป**
5. เปิดลิงก์นี้ = หน้าแรกของระบบ (บุ๊กมาร์ก/ปักหมุดไว้ที่ครัวกลางได้เลย)

> ⚠️ ทุกครั้งที่แก้โค้ด ต้องกด **Deploy → Manage deployments → (ดินสอ) → Version: New version → Deploy** เพื่อให้เว็บอัปเดต

---

## ส่วนที่ 4 — ตั้งค่า LINE Official Account (Messaging API)

> ### ♻️ มีระบบแจ้ง "วันหมดอายุ" บน LINE อยู่แล้ว? ใช้ร่วมกันได้เลย
> - **ใช้กลุ่ม LINE เดิม** ได้ (กลุ่มเดียวรับได้ทั้งของเข้าและวันหมดอายุ)
> - **ถ้าระบบเดิมเป็น Messaging API (OA):** ใช้ **Channel access token เดิม** ได้ → ข้ามข้อ 4.1 ไปข้อ 4.2 เลย
>   (กด **Copy** เท่านั้น **อย่ากด Issue ใหม่** ไม่งั้น token เดิมใช้ไม่ได้ ระบบเดิมจะพัง)
> - ⚠️ **อย่าแตะช่อง Webhook URL** ของระบบเดิม — ระบบนี้ไม่ใช้ webhook เลย จึงไม่ไปทับกัน
> - **Group ID:** เอาของเดิมมาวางในชีต `สถานที่` ได้ตรง ๆ (ข้อ 4.3)
> - **ถ้าระบบเดิมเป็นบริการอื่น** (Make/Zapier/บอทเจ้าอื่น): สร้าง OA ใหม่ตามข้อ 4.1 แล้วเชิญเข้ากลุ่มเดิม — มี 2 บอทในกลุ่มได้ ต่างคนต่างแจ้ง

### 4.1 สร้าง OA และเปิด Messaging API  *(ข้ามได้ถ้าจะใช้ OA เดิม)*
1. ไป https://developers.line.biz/console/ ล็อกอินด้วยบัญชี LINE
2. สร้าง **Provider** (ชื่ออะไรก็ได้ เช่น "หม่าล่า")
3. สร้าง **Channel → Messaging API** (จะได้ Official Account มาด้วย)
4. เข้าแท็บ **Messaging API**:
   - เลื่อนหา **Channel access token (long-lived)** → กด **Issue** → คัดลอก token เก็บไว้
5. แท็บ **Messaging API** ปิดสองอย่างนี้เพื่อกันบอทตอบมั่ว: **Auto-reply messages = Disabled**, **Greeting messages** ตามใจ
   (ตั้งได้ที่ปุ่มลิงก์ "LINE Official Account Manager")

### 4.2 ใส่ token เข้าสคริปต์
1. ที่หน้า Apps Script → เมนูซ้าย **Project Settings (เฟือง) → Script Properties → Add script property**
2. ใส่:
   - **Property:** `LINE_TOKEN`
   - **Value:** วาง Channel access token (จะเป็น token ของ OA เดิมหรือ OA ใหม่ก็ได้)
3. กด **Save**

### 4.3 ใส่ Group ID ลงชีต `สถานที่`

เอา **LINE Group ID** (ขึ้นต้นด้วย `C`) ของแต่ละกลุ่ม ไปวางในชีต **`สถานที่`** ให้ตรงแถว

- **กลุ่มครัวกลาง** → แถวประเภท `ครัวกลาง`
- **กลุ่มแต่ละสาขา** → แถวประเภท `สาขา`

> Group ID หาได้จากการตั้งค่าของระบบแจ้งวันหมดอายุเดิมที่คุณใช้อยู่
> ระบบนี้ **ไม่ได้ตั้ง webhook** จึงไม่ไปทับของเดิม — และไม่มีฟังก์ชันจับ Group ID ให้อัตโนมัติ

### 4.5 ทดสอบการเชื่อมต่อ
กด **🌶️ ระบบสต็อก → 🧪 ทดสอบส่ง LINE ทุกสาขา**
ระบบจะส่งข้อความทดสอบเข้าทุกกลุ่ม แล้วสรุปผลว่าสาขาไหนสำเร็จ/ไม่สำเร็จ
(เป็นการ push อย่างเดียว **ไม่กระทบ webhook หรือระบบแจ้งวันหมดอายุเดิม**)

---

## ✅ เสร็จแล้ว! ทดสอบให้ครบหนึ่งรอบ

1. เมนู **🧪 ทดสอบส่ง LINE ทุกกลุ่ม** → ต้องขึ้น ✅ ครบทุกแถวในชีต `สถานที่`
2. เปิดลิงก์เว็บ → ล็อกอิน `kitchen`
3. **📥 ของเข้าครัวกลาง** → เลือกไส้กรอกหนังกรอบ → กรอก `3` แพ็ค `5` ไม้
   → หน้าจอโชว์ "3 แพ็ค 5 ไม้" → กลุ่มครัวกลางได้ข้อความ
4. **🏪 ของเข้าร้าน** → สาขา 1 → ไส้กรอกหนังกรอบ `1` แพ็ค
   → กลุ่มสาขา 1 ได้ข้อความ "1 แพ็ค (1 แพ็ค = 7 ไม้)" พร้อมวันเข้า/วันหมดอายุ
   → หน้าแรกโชว์ครัวกลางเหลือ **2 แพ็ค 5 ไม้**
5. **📋 เช็คสต็อกรายสัปดาห์** → เลือกครัวกลาง → กรอกยอดนับ → ดูส่วนต่าง
6. **🗑️ ของเสีย** → ตัดออก 1 ไม้ → เช็คว่ายอดลดจริง
7. เมนู **⏰ ทดสอบแจ้งวันหมดอายุเดี๋ยวนี้** → ถ้ามีของหมดอายุวันนี้จะยิงเข้ากลุ่ม
8. ลบรายการทดสอบออกจากชีต `ของเข้าครัวกลาง` / `ของเข้าร้าน` / `ของเสีย` / `เช็คสต็อก`

### ⚠️ เตือนของครัวกลางใกล้หมด

ตั้งจุดเตือนต่อสินค้าที่คอลัมน์ **เตือนเมื่อเหลือ(แพ็ค)** (ตั้งต้นให้ 10 ทุกตัว)

- **แจ้งทันที** เมื่อของตกลงมาถึง/ต่ำกว่าจุดเตือน — ตอนส่งเข้าร้าน, บันทึกของเสีย, หรือปรับยอดตอนนับสต็อก
- **แจ้งครั้งเดียว ไม่สแปม** — หยิบของออกอีกก็ไม่เตือนซ้ำ จนกว่าจะเติมของกลับขึ้นเหนือจุดเตือนแล้วตกลงมาใหม่
- **สรุปทุกเช้า** รายการที่ยังต่ำกว่าจุดเตือนทั้งหมด
- เข้า **กลุ่มครัวกลาง** เท่านั้น
- ใส่ `0` = ปิดการเตือนสินค้านั้น

ตัวอย่างข้อความ:
```
⚠️ ของครัวกลางใกล้หมด
• ฟองเต้าหู้ม้วน  เหลือ 10 แพ็ค  (จุดเตือน 10 แพ็ค)

เตรียมเสียบเพิ่มหรือสั่งของได้แล้ว
```

หน้าแรกจะขึ้นป้าย ⚠️ ที่รายการนั้นด้วย

### 🔎 ตรวจเช็คของหาย — ใช้ยังไง

1. **บันทึกยอด POS ทุกวัน** (เมนู 💵) — เงินเข้า + ส่วนลดที่ให้ไป
2. **น้ำจิ้ม/ของที่แถมฟรี** บันทึกที่เมนู 🗑️ เลือกประเภท **"แถมฟรี"**
   (หักที่จำนวนของ ไม่ใช่เงิน — ตามที่ร้านใช้จริง)
3. **นับสต็อกทุกสัปดาห์** (เมนู 📋) — ต้องนับอย่างน้อย 2 ครั้งถึงเทียบได้
4. เปิดเมนู 🔎 เลือกสถานที่ + ช่วงวันที่ → กด **ดูรายงาน**

```
ขายได้(ไม้) = นับต้นงวด + ของเข้า − ของเสีย − แถมฟรี − นับปลายงวด
ยอดที่ควรได้ = Σ(ขายได้ × ราคา) − ส่วนลด
ส่วนต่าง     = เงินเข้าจริง − ยอดที่ควรได้   ← ติดลบ = เงินขาด
```

รายงานแสดงแยกรายสินค้าด้วย ว่าตัวไหนหายไปเท่าไหร่

### วงจรข้อมูล
```
📥 ของเข้าครัวกลาง ──▶ สต็อกครัวกลาง ──▶ 🏪 ของเข้าร้าน ──▶ สต็อกสาขา
                                                              │
                   🗑️ ของเสีย (ตัดออก) ◀────────┬─────────────┤
                   📋 เช็คสต็อก (ปรับให้ตรง) ◀───┘             │
                                        ⏰ แจ้งวันหมดอายุ ◀────┘
```

## ❓ ปัญหาที่พบบ่อย

| อาการ | วิธีแก้ |
|---|---|
| บอทไม่ตอบ "id" ในกลุ่ม | ปิด Auto-reply ใน OA Manager, เปิด Use webhook, เช็ค Webhook URL ลงท้าย `/exec` |
| เว็บขึ้น "ยังไม่ได้ส่ง LINE" | ยังไม่ได้ใส่ `LINE_TOKEN` หรือช่อง LINE Group ID ในชีตสาขายังว่าง |
| แก้โค้ดแล้วเว็บไม่เปลี่ยน | ต้อง Deploy เวอร์ชันใหม่ (Manage deployments → New version) |
| ไม่โชว์ "ควรแพ็คได้กี่ถุง" | สินค้านั้นยังไม่ได้ใส่ค่า "กรัมต่อถุง" ในชีต `รายการสินค้า` |
| เปิดลิงก์แล้วให้ล็อกอินด้วย Google | ตอน Deploy ต้องตั้ง Who has access = **Anyone** |
| ลืมรหัสผ่านพนักงาน | พิมพ์รหัสใหม่ทับในชีต `ผู้ใช้งาน` ช่องรหัสผ่าน (ทับ hash ได้เลย) |
| สาขาขึ้น "ไม่มีสิทธิ์" | ช่อง "สาขา" ในชีต `ผู้ใช้งาน` สะกดไม่ตรงกับชีต `สถานที่` |
| ไม่ได้รับแจ้งวันหมดอายุ | ยังไม่ได้กดเมนู **2) เปิดแจ้งเตือนวันหมดอายุอัตโนมัติ** |
| ยอดสต็อกไม่ตรง | ใช้เมนู 📋 เช็คสต็อกรายสัปดาห์ ติ๊ก "ปรับสต็อกให้ตรงกับที่นับได้" |

## 🔒 ความปลอดภัย — อ่านก่อนใช้จริง
- **เปลี่ยนรหัสผ่านเริ่มต้น (`1234`) ทุกบัญชีก่อนเปิดใช้งานจริง** ไม่งั้นใครเดาได้ก็เข้าระบบได้
- ลิงก์เว็บต้องตั้งเป็น "Anyone" เพื่อให้เปิดจาก LINE ได้ → **ความปลอดภัยจึงอยู่ที่รหัสผ่าน** ตั้งให้เดายาก และอย่าส่งลิงก์+รหัสไว้ในกลุ่มเดียวกัน
- รหัสผ่านถูกเก็บเป็น hash (SHA-256 + salt) ไม่ได้เก็บรหัสจริง
- **ใครเข้าถึง Google Sheet ได้ = เห็นและแก้ข้อมูลทั้งหมดได้** โดยไม่ต้องผ่านหน้าล็อกอิน → แชร์ชีตเฉพาะแอดมินเท่านั้น
- Channel access token = รหัสลับ อย่าเผยแพร่ อย่า commit ลง GitHub (เก็บใน Script Properties เท่านั้น)
- พนักงานที่ลาออก: เอาเครื่องหมายถูก "เปิดใช้งาน" ออก แล้วกดเมนู **🔒 บังคับล็อกอินใหม่ทุกคน**
